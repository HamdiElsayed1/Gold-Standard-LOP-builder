# Output

- **`scratch/`** — Iterative PPT/HTML (and other exports) while drafting.
- **`final/`** — Gate C sign-off versions only. Use dated subfolders (`YYYY-MM-DD`) or `v0.1`, `v0.2` inside `final/` as your squad prefers.
