# Assembler — PPT + HTML (v0.1)

**Role:** Assembler — packaging spec only; **no new facts**.

**Inputs:** Narrative after **LOP coach** + **human-approved** revisions only (then package — no new facts).

**Tasks:**

1. **PPT spec:** ordered slide list with `Title` + `Bullets` + `Notes` + `Appendix refs`.  
2. **HTML spec:** `<section id="...">` outline mirroring spine; paste-ready narrative blocks (markdown inside sections is fine for v0.1).  
3. Contents table / cross-refs between PPT order and HTML ids.

**Output:** Two subsections in one document — **PPT pack** then **HTML pack**.
